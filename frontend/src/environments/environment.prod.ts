export const environment = {
  production: true,
  // apiUrl: '/api', //local proxy in development, same domain in production
  apiUrl: 'https://supermarket-app-c1su.onrender.com/api', //production API URL (other domain form render)
};
